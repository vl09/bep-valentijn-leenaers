# Results

This chapter presents the experimental results and key observations.

$$ F = m \cdot \frac{dv}{dt} $$(eq:newton2)

Using a labeled equation, `$$ F = m \cdot \frac{dv}{dt} $$(eq:newton2)` we can refer to that equation like: {numref}`eq {number} <eq:newton2>`. In your `myst.yml` you can specify the numbering, see the [documentation](https://mystmd.org/guide/math#customizing-numbering).